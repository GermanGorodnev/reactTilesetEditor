import { TILESET_SETTINGS } from "constants.js"

const initialState = {
};
export default function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            break;
    }

    return state;
} 